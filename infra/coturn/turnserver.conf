listening-port=3478
fingerprint
lt-cred-mech
realm=desklink.local
user=desklink:CHANGE_ME_NOW
no-multicast-peers
no-cli
stale-nonce=600
min-port=49160
max-port=49200

# Production requirements:
# 1. Set external-ip=<PUBLIC_IP> when behind NAT.
# 2. Replace the static user with TURN REST API temporary credentials.
# 3. Add TLS cert/key and expose 5349 for turns: where required.
# 4. Open UDP/TCP 3478 and the relay range above in the firewall/security group.
