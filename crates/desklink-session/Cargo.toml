[package]
name = "desklink-session"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
desklink-protocol = { path = "../desklink-protocol" }

[dev-dependencies]
criterion = "=0.5.1"

[[bench]]
name = "session_bench"
harness = false
